---
documentation:
  title: test
  description: test
  authors:
    - schlund_manuel

datasets:
  # - {project: CMIP6, dataset: NICAM16-9S, exp: highresSST-present, ensemble: r1i1p1f1, grid: gr, timerange: 1950/1960}
  - {project: CMIP6, dataset: MPI-ESM1-2-XR, exp: hist-1950, ensemble: r1i1p1f1, grid: gn, timerange: 1950/2014}

preprocessors:
  test:
    area_statistics:
      operator: mean
      # weights: false  # with v2.11.1 this works fine, with main it doesn't work regardless of this setting
    # climate_statistics:
    #   operator: mean
    #   # weights: false
    # axis_statistics:
    #   operator: mean
    #   axis: X
    #   # weights: false

diagnostics:
  test:
    variables:
      tas:
        mip: day
        preprocessor: test
    scripts:
      null
